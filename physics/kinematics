<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Kinematics – PromptSchola Physics</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta
    name="description"
    content="Kinematics track in PromptSchola Physics: motion in 1D and 2D, constant acceleration, projectile motion, relative motion, and uniform circular motion — all built as guided nano-lessons."
  />

  <!-- Inter font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg-body: #f3f4f6;
      --bg-surface: #f9fafb;
      --bg-card: #ffffff;
      --accent: #7e22ce;
      --accent-strong: #6d28d9;
      --accent-soft: rgba(126, 34, 206, 0.09);
      --accent-soft2: rgba(129, 140, 248, 0.12);
      --text-main: #111827;
      --text-muted: #4b5563;
      --text-subtle: #9ca3af;
      --text-strong: #020617;
      --border-subtle: rgba(209, 213, 219, 0.55);
      --radius-card: 18px;
      --radius-pill: 999px;
      --shadow-soft: 0 18px 40px rgba(15, 23, 42, 0.12);
      --shadow-card: 0 16px 35px rgba(15, 23, 42, 0.12);
      --font-main: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      --transition-fast: 0.25s ease-out;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: var(--font-main);
      background: radial-gradient(circle at top, #e5e7eb, #f9fafb 45%, #f3f4f6);
      font-size: 16.5px;
      color: var(--text-main);
      -webkit-font-smoothing: antialiased;
      line-height: 1.7;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    .page {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      width: 100%;
      max-width: 1120px;
      margin: 0 auto;
      padding: 0 1.4rem;
    }

    /* Header / Nav – canonical */
    header {
      position: sticky;
      top: 0;
      z-index: 40;
      backdrop-filter: blur(14px);
      background: linear-gradient(
        to bottom,
        rgba(249, 250, 251, 0.96),
        rgba(249, 250, 251, 0.94),
        rgba(249, 250, 251, 0.9)
      );
      border-bottom: 1px solid rgba(209, 213, 219, 0.9);
    }

    .nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.75rem 0;
    }

    .nav-left {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .logo-mark {
      width: 34px;
      height: 34px;
      border-radius: 40%;
      background: radial-gradient(circle at 30% 30%, var(--accent), var(--accent-strong));
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      font-size: 1rem;
      color: #fff;
    }

    .logo-text span:first-child {
      display: block;
      font-weight: 700;
      font-size: 1.02rem;
      letter-spacing: 0.02em;
      color: var(--text-strong);
    }

    .logo-text span:last-child {
      display: block;
      font-size: 0.76rem;
      color: var(--text-subtle);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 1.3rem;
      font-size: 0.95rem;
    }

    .nav-links a {
      color: var(--text-muted);
      opacity: 0.9;
      transition: opacity var(--transition-fast), color var(--transition-fast);
    }

    .nav-links a:hover {
      opacity: 1;
      color: var(--accent-strong);
    }

    .nav-right {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-size: 0.9rem;
    }

    .btn-pill {
      border-radius: var(--radius-pill);
      padding: 0.45rem 0.95rem;
      font-size: 0.86rem;
      border: 1px solid rgba(209, 213, 219, 0.9);
      background: #ffffff;
      color: var(--text-main);
      cursor: pointer;
      transition: background var(--transition-fast), transform var(--transition-fast), box-shadow var(--transition-fast), border-color var(--transition-fast);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      white-space: nowrap;
    }

    .btn-pill:hover {
      background: #f3f4f6;
      transform: translateY(-1px);
      border-color: rgba(156, 163, 175, 0.9);
      box-shadow: 0 8px 20px rgba(148, 163, 184, 0.35);
    }

    .btn-primary {
      border-radius: var(--radius-pill);
      padding: 0.6rem 1.3rem;
      font-size: 0.9rem;
      font-weight: 600;
      border: none;
      cursor: pointer;
      background: radial-gradient(circle at 30% 0%, #a855f7, #7e22ce, #4c1d95);
      color: #f9fafb;
      box-shadow: 0 10px 22px rgba(79, 70, 229, 0.35);
      transition: transform var(--transition-fast), filter var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast);
      white-space: nowrap;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      filter: brightness(1.03);
      box-shadow: 0 14px 30px rgba(79, 70, 229, 0.45);
      background: radial-gradient(circle at 30% 0%, #c084fc, #7e22ce, #4c1d95);
    }

    /* HERO — centered with soft glow */
    .hero {
      position: relative;
      overflow: hidden;
      padding: 3.2rem 0 3rem;
      text-align: center;
    }

    .hero::before {
      content: "";
      position: absolute;
      inset: -80px;
      pointer-events: none;
      background:
        radial-gradient(circle at 10% 0%, rgba(129, 140, 248, 0.40), transparent 60%),
        radial-gradient(circle at 90% 40%, rgba(168, 85, 247, 0.35), transparent 55%);
      opacity: 0.9;
      z-index: -1;
    }

    .hero-kicker {
      font-size: 0.8rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--text-subtle);
      margin-bottom: 0.9rem;
    }

    .hero-title {
      font-size: clamp(2.2rem, 3.2vw, 2.8rem);
      font-weight: 800;
      color: var(--text-strong);
      margin-bottom: 0.6rem;
      max-width: 40rem;
      margin-left: auto;
      margin-right: auto;
      line-height: 1.12;
    }

    .hero-subtitle {
      font-size: 1.1rem;
      max-width: 40rem;
      margin: 0 auto;
      color: var(--text-muted);
    }

    /* TOPICS HEADING — centered */
    .topics-heading {
      margin: 2.8rem auto 1.7rem;
      text-align: center;
      max-width: 40rem;
    }

    .topics-heading-kicker {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--text-subtle);
      margin-bottom: 0.35rem;
    }

    .topics-heading-title {
      font-size: 1.85rem;
      font-weight: 800;
      color: var(--text-strong);
      margin-bottom: 0.4rem;
    }

    .topics-heading-subtitle {
      font-size: 1.05rem;
      color: var(--text-muted);
      margin: 0 auto;
    }

    /* TOPIC CARDS */
    .topics-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.2rem;
      margin-top: 1.4rem;
    }

    .topic-card {
      background: var(--bg-card);
      border-radius: var(--radius-card);
      border: 1px solid var(--border-subtle);
      padding: 1.1rem 1.15rem 1.2rem;
      box-shadow: var(--shadow-card);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-height: 210px;
    }

    .topic-kicker {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--text-subtle);
      margin-bottom: 0.2rem;
    }

    .topic-title {
      font-size: 1.2rem;
      font-weight: 700;
      color: var(--text-strong);
    }

    .topic-body {
      font-size: 1.05rem;
      color: var(--text-muted);
      margin-top: 0.4rem;
      line-height: 1.55;
    }

    .topic-list {
      list-style: disc;
      margin-top: 0.45rem;
      padding-left: 1.35rem;
      font-size: 1.05rem;
      color: var(--text-muted);
      line-height: 1.55;
    }

    .topic-list li {
      margin-bottom: 0.22rem;
    }

    .topic-footer {
      margin-top: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.82rem;
      color: var(--text-subtle);
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    /* Footer – canonical */
    footer {
      margin-top: auto;
      padding: 1.8rem 0 2.1rem;
      font-size: 0.86rem;
      color: var(--text-subtle);
    }

    .footer-inner {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      gap: 0.9rem;
      align-items: center;
      border-top: 1px solid rgba(209, 213, 219, 0.9);
      padding-top: 0.95rem;
    }

    /* Responsive */
    @media (max-width: 960px) {
      .topics-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 800px) {
      .topics-grid {
        grid-template-columns: 1fr;
      }
      .nav-links {
        display: none;
      }
      .hero-title {
        font-size: 2.1rem;
      }
    }

    @media (max-width: 520px) {
      .topic-card {
        min-height: 0;
      }
    }
  </style>
</head>

<body>
<div class="page">

  <!-- NAV -->
  <header>
    <div class="container">
      <div class="nav">
        <div class="nav-left">
          <div class="logo-mark">P</div>
          <div class="logo-text">
            <span>PromptSchola</span>
            <span>Prompt-based STEM subjects mastery</span>
          </div>
        </div>

        <nav class="nav-links">
          <a href="../physics.html">Physics</a>
          <a href="../math.html">Math</a>
          <a href="../chemistry.html">Chemistry</a>
          <a href="../biology.html">Biology</a>
          <a href="../pricing.html">Pricing</a>
        </nav>

        <div class="nav-right">
          <span id="nav-user" style="font-size:0.78rem;color:#9ca3af;"></span>

          <!-- Logged OUT -->
          <a id="nav-login-btn" href="../auth.html" class="btn-pill">Sign In</a>
          <a id="nav-register-btn" href="../auth.html?mode=signup" class="btn-primary">Register for Free</a>

          <!-- Logged IN -->
          <button id="nav-signout-btn" class="btn-pill" style="display:none;">Sign out</button>
        </div>
      </div>
    </div>
  </header>

  <!-- HERO -->
  <main>
    <section class="hero">
      <div class="container">
        <div class="hero-kicker">Kinematics · Physics</div>
        <h1 class="hero-title">Describe motion clearly in one and two dimensions.</h1>
        <p class="hero-subtitle">
          Build intuition for how objects move: 1D and 2D motion, constant acceleration, projectile motion,
          relative motion, and uniform circular motion — all broken into guided nano-lessons.
        </p>
      </div>
    </section>

    <!-- TOPICS -->
    <section>
      <div class="container">
        <div class="topics-heading">
          <div class="topics-heading-kicker">Kinematics topics</div>
          <h2 class="topics-heading-title">Build intuition for motion, step by step</h2>
          <p class="topics-heading-subtitle">
            Work through these topics in order, or jump to the motion model you need for your current problem set.
          </p>
        </div>

        <div class="topics-grid">

          <!-- 1. Describing Motion in One Dimension -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">Start here</div>
              <div class="topic-title">Describing Motion in One Dimension</div>
              <div class="topic-body">
                Learn how to describe motion along a straight line using displacement, velocity, and acceleration.
                <ul class="topic-list">
                  <li>Displacement vs distance</li>
                  <li>Average vs instantaneous velocity</li>
                  <li>Acceleration</li>
                  <li>Motion graphs (x–t, v–t, a–t)</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="kinematics-describing-1d.html" class="btn-primary">Start topic</a>
              <span>Core language for all later motion.</span>
            </div>
          </article>

          <!-- 2. Constant Acceleration in 1D -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">Key model</div>
              <div class="topic-title">Constant Acceleration in 1D</div>
              <div class="topic-body">
                Use the kinematic (SUVAT) equations to analyze motion with constant acceleration, including free fall.
                <ul class="topic-list">
                  <li>Derivation of kinematic equations</li>
                  <li>Free-fall motion</li>
                  <li>Applications to real-world motion</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="kinematics-constant-acceleration-1d.html" class="btn-primary">Start topic</a>
              <span>Bridge between graphs and equations.</span>
            </div>
          </article>

          <!-- 3. Two-Dimensional Motion -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">2D motion</div>
              <div class="topic-title">Two-Dimensional Motion</div>
              <div class="topic-body">
                Extend 1D ideas into motion with x and y components, preparing for projectile and circular motion.
                <ul class="topic-list">
                  <li>Motion in x and y components</li>
                  <li>Independence of horizontal and vertical motion</li>
                  <li>Projectile motion basics</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="kinematics-two-dimensional-motion.html" class="btn-primary">Start topic</a>
              <span>Foundation for projectile motion.</span>
            </div>
          </article>

          <!-- 4. Projectile Motion -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">Projectiles</div>
              <div class="topic-title">Projectile Motion</div>
              <div class="topic-body">
                Analyze objects launched into the air under uniform gravity, ignoring air resistance.
                <ul class="topic-list">
                  <li>Range, time of flight, maximum height</li>
                  <li>Projectile motion at angles</li>
                  <li>Symmetry of projectile trajectories</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="projectile-motion.html" class="btn-primary">Start topic</a>
              <span>Classic exam and real-world problems.</span>
            </div>
          </article>

          <!-- 5. Relative Motion -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">Frames of reference</div>
              <div class="topic-title">Relative Motion</div>
              <div class="topic-body">
                Learn how motion looks different from different reference frames using relative velocity.
                <ul class="topic-list">
                  <li>Relative velocity in one dimension</li>
                  <li>Relative velocity in two dimensions</li>
                  <li>Riverboat/airplane problems</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="relative-motion.html" class="btn-primary">Start topic</a>
              <span>Connect intuition to vector subtraction.</span>
            </div>
          </article>

          <!-- 6. Uniform Circular Motion -->
          <article class="topic-card">
            <div>
              <div class="topic-kicker">Circular motion</div>
              <div class="topic-title">Uniform Circular Motion (Kinematics Only)</div>
              <div class="topic-body">
                Describe motion in a circle using angular variables and centripetal acceleration — before forces.
                <ul class="topic-list">
                  <li>Angular variables vs linear variables</li>
                  <li>Centripetal acceleration</li>
                  <li>Period and frequency of rotation</li>
                </ul>
              </div>
            </div>
            <div class="topic-footer">
              <a href="uniform-circular-motion.html" class="btn-primary">Start topic</a>
              <span>Prepares you for circular dynamics.</span>
            </div>
          </article>

        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <div class="footer-inner">
        <span>© 2025 PromptSchola. All rights reserved.</span>
        <div></div>
      </div>
    </div>
  </footer>
</div>

<!-- Supabase + auth helpers -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="../auth.js"></script>

<script>
  document.addEventListener('DOMContentLoaded', async () => {
    if (typeof updateNavUserDisplay === 'function') {
      updateNavUserDisplay();
    }

    const loginBtn = document.getElementById('nav-login-btn');
    const registerBtn = document.getElementById('nav-register-btn');
    const signOutBtn = document.getElementById('nav-signout-btn');

    if (typeof getCurrentUser === 'function') {
      const user = await getCurrentUser();

      if (user) {
        if (loginBtn) loginBtn.style.display = 'none';
        if (registerBtn) registerBtn.style.display = 'none';
        if (signOutBtn) signOutBtn.style.display = 'inline-flex';
      } else {
        if (loginBtn) loginBtn.style.display = 'inline-flex';
        if (registerBtn) registerBtn.style.display = 'inline-flex';
        if (signOutBtn) signOutBtn.style.display = 'none';
      }
    }

    if (signOutBtn && typeof signOutUser === 'function') {
      signOutBtn.addEventListener('click', signOutUser);
    }
  });
</script>

</body>
</html>
